<!DOCTYPE html>
<html lang="en">
    <%- include ('partials/head') %>
<body>
    <%- include ('partials/header') %>
    <main class="detalle-main">
        <%- include ('partials/sideBar') %>
        <div class="productoSections">
        <section class="seciphone" id="iphone">
            
            <p class="productoCategoria">iPhone</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catIphone.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catIphone[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catIphone[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catIphone[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catIphone[i].precio) %></h3>
                                <p class="productodescripcion"><%= catIphone[i].descripcion %></p>
                        </a>
                    </div>
                <% } %>           
            </div>

        </section>

        <section class="seciphone" id="mac">
            
            <p class="productoCategoria">Mac</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catMac.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catMac[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catMac[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catMac[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catMac[i].precio) %></h3>
                                <p class="productodescripcion"><%= catMac[i].descripcion %></p>
                        </a>
                    </div>
                <% } %>           
            </div>

        </section>

        <section class="seciphone" id="airpods">
            
            <p class="productoCategoria">AirPods</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catAirpods.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catAirpods[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catAirpods[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catAirpods[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catAirpods[i].precio) %></h3>
                                <p class="productodescripcion"><%= catAirpods[i].descripcion %></p>
                        </a>
                    </div>
                <% } %>           
            </div>

        </section>

        <section class="seciphone" id="ipad">
            
            <p class="productoCategoria">iPad</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catIpad.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catIpad[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catIpad[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catIpad[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catIpad[i].precio) %></h3>
                                <p class="productodescripcion"><%= catIpad[i].descripcion %></p>
                        </a>
                    </div>
                    
                <% } %>           
            </div>

        </section>

        <section class="seciphone" id="watch">
            
            <p class="productoCategoria">Watch</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catWatch.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catWatch[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catWatch[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catWatch[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catWatch[i].precio) %></h3>
                                <p class="productodescripcion"><%= catWatch[i].descripcion %></p>
                        </a>
                    </div>
                <% } %>           
            </div>

        </section>

        <section class="seciphone" id="accesorios">
            
            <p class="productoCategoria">Accesorios</p>

            <div class="productosListaIphone">
                <% for (let i = 0 ; i < catAccesorios.length ; i++) { %>
                    <div class="productos">
                        <a href="/productos/<%= catAccesorios[i].id %>">
                            <div class="divproducto">
                                <img class="productoimg" src="<%= catAccesorios[i].imagen %>">
                            </div>
                                <h2 class="productonombre"><%= catAccesorios[i].nombre %></h2>
                                <h3 class="productoprecio">$ <%= toThousand(catAccesorios[i].precio) %></h3>
                                <p class="productodescripcion"><%= catAccesorios[i].descripcion %></p>
                        </a>
                    </div>
                <% } %>           
            </div>

        </section>
    </div>


    </main>    
    <%- include ('partials/footer') %>
</body>

</html> 